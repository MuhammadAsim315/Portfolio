# Muhammad Asim — Portfolio

Personal portfolio built with **Vite + React**.

## Run locally

```bash
npm install
npm run dev
```

Then open the URL shown in the terminal (usually `http://localhost:5173`).

## Build

```bash
npm run build
npm run preview
```

## Resume

The resume download link uses `My_Resume.pdf` in the project root.

